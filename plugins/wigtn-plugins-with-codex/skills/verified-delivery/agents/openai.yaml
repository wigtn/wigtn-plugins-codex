interface:
  display_name: "WIGTN Verified Delivery"
  short_description: "Run an explicit implementation and verification workflow"
  default_prompt: "Use $wigtn-plugins-with-codex:verified-delivery to implement and verify this feature end to end."
policy:
  allow_implicit_invocation: true
