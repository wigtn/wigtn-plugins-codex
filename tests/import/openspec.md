# Dark Mode

## ADDED Requirements

### Requirement: Theme selection

The application SHALL let a user switch between light and dark themes.

#### Scenario: User selects dark mode

- **WHEN** the user selects dark mode
- **THEN** the application persists the choice

### Requirement: System default

The application MUST use the system preference when no choice exists.
